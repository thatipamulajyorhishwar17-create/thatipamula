# Server
PORT=5000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=project_solovers

# JWT
JWT_SECRET=project_solovers_jwt_secret_key_change_in_production

# SMTP (Gmail with App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=projectsolver@c24e.co
SMTP_PASS=your_app_password_here
